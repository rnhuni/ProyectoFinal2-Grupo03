import { Box, Heading } from "@chakra-ui/react";

const Roles = () => {
  return (
    <Box p={4}>
      <Heading as="h1">Roles Page</Heading>
      <p>This is the Roles page where you can assign and manage roles.</p>
    </Box>
  );
};

export default Roles;
