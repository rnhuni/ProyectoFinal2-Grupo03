import { Box, Heading } from "@chakra-ui/react";

const Incidents = () => {
  return (
    <Box p={4}>
      <Heading as="h1">Incidents Page</Heading>
      <p>This is the Incidents page where you can track all incidents.</p>
    </Box>
  );
};

export default Incidents;
