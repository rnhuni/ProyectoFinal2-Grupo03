name: Run Unit Tests for all Apps

on:
  pull_request:
    branches:
      - main
    paths:
      - 'web-app/**'

jobs:
  test-web-app:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install dependencies
        run: |
          cd web-app
          npm install

      - name: Run Unit Tests for web-app
        run: |
          cd web-app
          npm run test -- --passWithNoTests
